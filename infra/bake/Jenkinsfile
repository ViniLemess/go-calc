pipeline {
    agent any
    stages {
        stage('Build go-calc') {
            steps {
                sh "cd go-calc && go build"
            }
        }
        stage('Build Docker Image') {
            steps {
                sh "packer build infra/bake/packer-template.pkr.hcl"
            }
        }
    }
}